<%= form_for(@schedule_plan) do |f| %>
  <% if @schedule_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule_plan.errors.count, "error") %> prohibited this schedule_plan from being saved:</h2>

      <ul>
      <% @schedule_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.datetime_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :doctor_id %><br />
    <%= f.number_field :doctor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
